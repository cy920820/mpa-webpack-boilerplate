@import './reset.styl'
@import './widgets/nav.styl'

.container {
  width: 1080px;
  margin: 30px auto 0;
  background-color: #fff;
}

.content {
  text-align: center;
  padding-bottom: 50px;

  img {
    width: 50%;
  }
}
